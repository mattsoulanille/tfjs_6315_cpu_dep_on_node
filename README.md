# Attempted Reproduction of [tfjs 6315](https://github.com/tensorflow/tfjs/issues/6315)

First, install dependencies by running:
```bash
yarn
```
Then, run the esbuild version with

```bash
yarn serve-esbuild
```

or the rollup version with
```bash
yarn serve-rollup
```
